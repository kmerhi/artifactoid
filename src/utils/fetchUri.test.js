// const fetchUri = require('./fetchUri');

describe('fetchUri', () => {

	it('should fetch uri', () => {
		// expect.assertions(1);
		// return fetchUri('http://somwhere/', {
		// 	user: 'bob',
		// 	pass: 'vila'
		// }).then(data => {
		// 	expect(data).toBe('peanut butter');
		// });
	});

});